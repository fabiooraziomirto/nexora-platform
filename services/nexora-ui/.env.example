# For local dev (npm run dev) without Docker.
# The Vite proxy in vite.config.ts handles /api/v2/* automatically,
# so these are only needed for a custom setup.
VITE_DEVICE_SERVICE_URL=
VITE_FLEET_SERVICE_URL=
