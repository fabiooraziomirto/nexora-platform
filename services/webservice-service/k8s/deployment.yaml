apiVersion: apps/v1
kind: Deployment
metadata:
  name: webservice-service
  namespace: stack4things
spec:
  replicas: 1
  selector:
    matchLabels:
      app: webservice-service
  template:
    metadata:
      labels:
        app: webservice-service
    spec:
      containers:
        - name: webservice-service
          image: ghcr.io/stack4things/webservice-service:latest
          ports:
            - containerPort: 8000
          readinessProbe:
            httpGet:
              path: /ready
              port: 8000
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
